{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "panel",
  "description": "The panel window (Ask, Attention, Recent). Listens to shell events and calls app commands; no plugin or core surface beyond that (docs/shell-architecture.md §2, §7).",
  "windows": ["panel"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:window:allow-scale-factor"
  ]
}
